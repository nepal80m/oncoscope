{
  "name": "oncoscope-mock-backend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Dev/mock backend for Oncoscope. Implements the NON-AI endpoints for real (catalog, annotations, comments, reviews, uploads, reports) and stubs the AI analysis with canned data. Mergeable into the real backend.",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.2"
  }
}
